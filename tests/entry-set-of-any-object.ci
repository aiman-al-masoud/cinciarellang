C = class{ x =1; y = 2; f = \x->2*x;  };

entries = C.entries();

keys = for e in entries -> e[0];
values = for e in entries -> e[1];


for k in keys, i in 0 to keys.size  {
		
		if k == 'f'{
			c = i;
		}
}

	
C.f == values[c];

