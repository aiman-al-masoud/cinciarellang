Vector = class{

	__init__ = \ list ->{
		this.list = list;
	}

	__add__ = \ other -> for i in list, 
							 j in other.list -> i+j;

}


v1 = Vector([-1,-2,-3]);
v2 = Vector([1,2,3]);

v1+v2 == [0,0,0];

