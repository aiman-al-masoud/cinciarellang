import GameLogic from "game-logic.ci";
import f from 'util.ci';
import Gui from 'gui/gui.ci'; //PROBLEM: this goes down in hierarchy forever cannot swap with line above

g = GameLogic();
Gui == Gui;

g.myScore = 3;
g.iWon();